const FeedbackOpen: React.FC< { title:string, empty:boolean, options:string[], active?:number }> = ({title, empty, options, active }) => {

    return (
        <div className="feedback-open">
        
        </div>
    );
};

export default FeedbackOpen